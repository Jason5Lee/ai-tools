{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "config",
  "description": "Capability for the config window",
  "windows": ["config"],
  "permissions": [
    "log:default"
  ]
}
